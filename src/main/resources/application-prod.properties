# Data Source Settings
spring.datasource.url=jdbc:postgresql://dpg-d822tt83kofs73dsfb0g-a.singapore-postgres.render.com/money_manager_4d5v?sslmode=require
spring.datasource.username=money_manager_4d5v_user
spring.datasource.password=ubXvXnq0VntJnb9dypkgzhOM6jHI7Ugg
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Settings
# 'update' is fine for a project, but ensures it speaks PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Show complete stack traces
logging.level.root=DEBUG
logging.level.com.moneymanagerproject=TRACE
logging.level.org.springframework.security=TRACE
logging.level.org.springframework.web=TRACE

# Show full error response
server.error.include-message=always
server.error.include-stacktrace=always
server.error.include-exception=true
server.error.include-binding-errors=always

spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# Mail - explicitly set in prod too
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=${MAIL_FROM}

# URLs
app.activation.url=${BACKEND_URL}
money.manager.frontend.url=${FRONTEND_URL}
money.manager.backend.url=${BACKEND_URL}
